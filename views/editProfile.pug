extends layouts/main 

block content
    .form__container
        form(action=router.editProfile, method = "post")
            label(for="avatar") Avatar
            input(type="file", id="avatar", name="avatar")
